[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-conversation-improvement"
version = "0.1.0"
description = "A restrained visual-expression and image-memory plugin for Hermes Agent"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]